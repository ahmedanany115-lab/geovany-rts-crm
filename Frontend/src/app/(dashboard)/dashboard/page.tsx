export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        KPI cards, revenue chart, and activity feed land in Milestone 2 — this route exists now so
        the authenticated shell has somewhere to land after login.
      </p>
    </div>
  );
}
